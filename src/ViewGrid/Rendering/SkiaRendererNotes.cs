namespace ViewGrid.Rendering;

public static class SkiaRendererNotes
{
    public const string Status = "Skia/GPU renderer için API hazırlandı. Bu paket ek NuGet bağımlılığı eklemeden GDI+ ile çalışır; SkiaSharp.Views.WindowsForms eklenirse aynı render sözleşmesi üzerinden GPU backend bağlanabilir.";
}
