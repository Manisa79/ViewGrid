using System.ComponentModel;

namespace ViewGrid.Core;

public enum ViewGridFilterIconStyle
{
    Inherit = -1,
    Funnel = 0,
    Dot = 1,
    CustomImage = 2
}

public enum ViewGridSortGlyphStyle
{
    Inherit = -1,
    Chevron = 0,
    Triangle = 1,
    CustomImage = 2
}
